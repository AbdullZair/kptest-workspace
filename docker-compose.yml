version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: kptest-postgres
    environment:
      POSTGRES_DB: kptest
      POSTGRES_USER: kptest
      POSTGRES_PASSWORD: kptest_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./devops/backup:/backup
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kptest -d kptest"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - kptest-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: kptest-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - kptest-network

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: kptest-backend
    environment:
      SPRING_PROFILES_ACTIVE: dev
      DB_HOST: postgres
      DB_PASSWORD: kptest_password
      REDIS_HOST: redis
      JWT_SECRET: ${JWT_SECRET:-dev-secret-key-change-in-production}
      HIS_BASE_URL: http://his-mock:8081
      MAIL_HOST: ${MAIL_HOST:-smtp.gmail.com}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    volumes:
      - ./backend/logs:/app/logs
    networks:
      - kptest-network

  # Frontend Web
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: kptest-frontend
    environment:
      VITE_API_URL: http://localhost:8080/api/v1
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - kptest-network

  # HIS Mock (for development)
  his-mock:
    build:
      context: ./devops/his-mock
      dockerfile: Dockerfile
    container_name: kptest-his-mock
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - kptest-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  kptest-network:
    driver: bridge
