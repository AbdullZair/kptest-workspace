/**
 * Message Entity Types
 * Re-export message types for use in entities
 */
export type {
  Message,
  MessageThread,
  MessageAttachment,
  MessageFormData,
  ThreadFormData,
  MessagePriority,
  ThreadType,
} from '@features/messages/types'
